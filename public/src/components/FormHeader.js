function FormHeader(props) {
   return (
      // <h2 className="text-center">Create Job Form - Count: {props.the_count}</h2>
      <h2 className="text-center">Create Job Form</h2>
   )
}

export default FormHeader;